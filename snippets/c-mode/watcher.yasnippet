# -*- mode: snippet -*-
# name: print watcher start
# key: watcher
# --

#include "watcher.h"

static uint32_t $1_watcher_CB(struct watcher_ctx * ctx) {
	$0
}

#define ${1:$(upcase yas-text)}_WATCHER_PERIOD_MS ($3)

REGISTER_WATCHER($1_watcher, ${1:$(upcase yas-text)}_WATCHER_PERIOD_MS, $1_watcher_CB);
